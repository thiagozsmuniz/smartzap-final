############################################################
# SmartZap — Exemplo de variáveis de ambiente (.env)
#
# Como usar localmente:
# 1) Copie este arquivo para .env.local
# 2) Preencha os valores
# 3) Rode: npm run dev
#
# ⚠️ Segurança:
# - NÃO commite .env.local / .env / .env.vercel*
# - Nunca exponha chaves SECRET no frontend
############################################################

# ------------------------------------------------------------
# 🔐 SEGURANÇA / ADMIN (Recomendado)
# ------------------------------------------------------------
# Senha do painel (login). É a "senha mestra" do SmartZap.
# - Local: defina aqui no .env.local
# - Vercel: o wizard salva como variável de ambiente
MASTER_PASSWORD=sua_senha_forte_aqui

# Chave para acesso geral às rotas internas protegidas.
# Dica para gerar: openssl rand -base64 32
SMARTZAP_API_KEY=szap_sua_api_key_aqui

# Chave administrativa para operações sensíveis (ex.: setup, validações e ações críticas).
SMARTZAP_ADMIN_KEY=szap_admin_sua_admin_key_aqui

# (Opcional no local) Marca o setup como concluído.
# Em produção, normalmente é setado automaticamente pelo wizard na Vercel.
# SETUP_COMPLETE=true

# Origem permitida (CORS) para o frontend.
# Em produção, use a URL do seu domínio/app.
FRONTEND_URL=https://seu-app.vercel.app

# ------------------------------------------------------------
# 📦 QSTASH (Upstash) — Fila/Workflows (Obrigatório para campanhas)
# ------------------------------------------------------------
# Pegue em: https://console.upstash.com/qstash (seção Quickstart)
QSTASH_TOKEN=xxx

# Redis (Opcional - recomendado p/ escala: dedupe de webhooks)
# Upstash Redis (REST)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Webhook dedupe (Opcional)
# TTL em segundos p/ dedupe de status (default: 7 dias)
WHATSAPP_STATUS_DEDUPE_TTL_SECONDS=

# ------------------------------------------------------------
# 📊 UPSTASH (Opcional) — Monitoramento/estatísticas
# ------------------------------------------------------------
# Permite mostrar métricas/custos no dashboard.
# Pegue em: Upstash → Personal Settings → Management API
UPSTASH_EMAIL=voce@exemplo.com
UPSTASH_API_KEY=xxx

# ------------------------------------------------------------
# 📱 WHATSAPP CLOUD API (Meta)
# ------------------------------------------------------------
# Fonte: Meta Business / Developers
# - Token: pode ser temporário (para testes) ou permanente (produção)
WHATSAPP_TOKEN=seu_access_token
WHATSAPP_PHONE_ID=seu_phone_number_id
WHATSAPP_BUSINESS_ACCOUNT_ID=seu_business_account_id

# Usado para validar o webhook (se aplicável)
WHATSAPP_VERIFY_TOKEN=seu_verify_token

# ------------------------------------------------------------
# 🔎 META APP (Opcional) — Diagnóstico avançado
# ------------------------------------------------------------
# Permite validar o token via Graph API /debug_token.
# Útil quando você atende alunos e precisa confirmar: expiração, escopos, app_id etc.
# ⚠️ Segurança: NUNCA exponha o APP SECRET em logs/prints.
META_APP_ID=seu_app_id
META_APP_SECRET=seu_app_secret

# ------------------------------------------------------------
#  IA (Opcional)
# ------------------------------------------------------------
# Para geração de templates/conteúdo via Gemini.
GEMINI_API_KEY=sua_gemini_api_key

# ------------------------------------------------------------
# 🗄️ BANCO DE DADOS (Supabase)
# ------------------------------------------------------------
# O SmartZap está padronizado para **Supabase (PostgreSQL)**.
# Pegue em: Supabase Dashboard → Project Settings → API
# Pegue em: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co

# ✅ Chaves Supabase (padrão novo — 2025+)
# - Publishable: sb_publishable_... (pode ficar no frontend)
# - Secret:      sb_secret_... (SOMENTE backend)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
# Observação: o snippet do Supabase às vezes vem com este nome. O SmartZap aceita como alias.
# (Use um ou outro — não precisa setar os dois.)
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_xxx
SUPABASE_SECRET_KEY=sb_secret_xxx

# ------------------------------------------------------------
# 🌐 URL do App (Opcional)
# ------------------------------------------------------------
# Em geral você NÃO precisa setar.
# Se não existir, o sistema tenta detectar automaticamente:
# 1) VERCEL_PROJECT_PRODUCTION_URL (domínio estável da produção)
# 2) VERCEL_URL (URL específica do deploy)
#
# Só defina manualmente se você tiver domínio próprio:
# NEXT_PUBLIC_APP_URL=https://seu-dominio.com

# ------------------------------------------------------------
# 🧪 DEBUG (Opcional)
# ------------------------------------------------------------
# Habilita o painel de telemetria de latência na tela de campanha
# (broadcast → UI, DB realtime → UI e duração de refetch).
# Use apenas em dev/diagnóstico.
NEXT_PUBLIC_REALTIME_TELEMETRY=0

